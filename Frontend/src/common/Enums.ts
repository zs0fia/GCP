export enum FontSizes {
  MEDIUM,
  LARGE
}

export enum ColorThemes {
  LIGHT,
  DARK
}

export enum Destination {
  A,
  B
}

export enum MapType {
  TERRAIN,
  SATELLITE
}

export enum DialogVisibility {
  HIDDEN,
  VISIBLE
}

export enum Adaptions {
  CHANGE_FONT_SIZE,
  CHANGE_COLOR_THEME,
  CHANGE_MAP_TYPE,
  CHANGE_DIALOG_VISIBILITY
}

export enum Emotion {
  NEUTRAL,
  HAPPY,
  SAD,
  ANGRY,
  FEARFUL,
  DISGUSTED,
  SURPRISED,
}
